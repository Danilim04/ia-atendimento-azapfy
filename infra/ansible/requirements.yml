# Instale com: ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.posix # authorized_key
    version: '>=1.5.0'
  - name: community.general # módulo ufw
    version: '>=8.0.0'
